name: Grok development snapshot

on:
  push:
    branches: [feat/grok-subscription-global-pool]
    paths: [.github/workflows/grok-development-snapshot.yml, scripts/grok-apply.py]

permissions:
  contents: write

concurrency:
  group: grok-isolated-development
  cancel-in-progress: false

jobs:
  implementation:
    if: github.ref == 'refs/heads/feat/grok-subscription-global-pool'
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: agent_saas_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/agent_saas_test
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/grok-subscription-global-pool
          fetch-depth: 0
      - uses: ./.github/actions/setup-pnpm
      - uses: actions/setup-node@v5
        with:
          node-version: '22.23.1'
          cache: pnpm
      - name: Install pinned dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply reviewed implementation on the feature branch only
        if: hashFiles('scripts/grok-apply.py') != ''
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = feat/grok-subscription-global-pool
          python3 - <<'PY'
          from pathlib import Path
          p = Path('scripts/grok-apply.py')
          s = p.read_text().replace("needle='  const readOnly = !isPlatformAdmin;';assert needle in s", "needle='export function ModelManager() {';assert needle in s")
          p.write_text(s)
          PY
          python3 scripts/grok-apply.py
          python3 - <<'PY'
          from pathlib import Path
          p = Path('web/src/components/ModelManager/index.tsx')
          s = p.read_text()
          if s.count('resolveGroupProtocol') == 1:
              s = s.replace('resolveGroupProtocol, ', '').replace(', resolveGroupProtocol', '')
          p.write_text(s)
          PY
          rm scripts/grok-apply.py
          git add -A
          git -c user.name='OpenAI' -c user.email='noreply@openai.com' commit -m 'feat(grok): apply subscription implementation work package'
          git push origin HEAD:refs/heads/feat/grok-subscription-global-pool
          echo "Validated worktree SHA: $(git rev-parse HEAD)"
      - name: Check server types
        run: pnpm -F server typecheck
      - name: Check web and shared types
        run: pnpm -F web typecheck && pnpm -F @agent/shared typecheck
      - name: Subscription regression
        run: pnpm -F server exec vitest run codex grok subscription
      - name: Model manager UI regression
        if: always() && !cancelled()
        run: pnpm -F web exec vitest run src/components/ModelManager
