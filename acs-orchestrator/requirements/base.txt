# Base Python packages installed into each workspace runtime venv.
# This is the production Agent hand baseline, not a coding-only minimal image.
#
# ⚠️ venv 安装是 `pip install --no-index --find-links=<wheelhouse>` 纯离线：
# 本文件新增/升级任何包，必须随同一批 acs-sandbox 镜像重建（wheelhouse 收录新 wheel）
# 一起部署；只改 ECS 上的 base.txt 会让所有 workspace venv 重建失败（缺 wheel）。
requests>=2.31,<3
httpx>=0.27,<1
beautifulsoup4>=4.12,<5
lxml>=5,<7
numpy>=1.26,<3
pandas>=2.2,<3
openpyxl>=3.1,<4
XlsxWriter>=3.2,<4
python-docx>=1.1,<2
python-pptx>=1,<2
Pillow>=10,<13
Jinja2>=3.1,<4
Markdown>=3.6,<4
pypdf>=4,<7
PyMuPDF>=1.24,<2
SQLAlchemy>=2,<3
duckdb>=1.1,<2
PyMySQL>=1.1,<2
psycopg[binary]>=3.1,<4
python-dotenv>=1,<2
PyYAML>=6,<7
jieba>=0.42,<1
# Office skills（pptx/docx/xlsx，skills-pool 上游快照）脚本工具链依赖（2026-07-16 生产反馈修复）：
# defusedxml — unpack/pack/clean/validate/thumbnail 等所有 Office XML 脚本的安全解析层
# markitdown — pptx/docx 文本提取与 Content QA（extras 对应三个 Office skill 的文件格式）
defusedxml>=0.7.1,<1
markitdown[pptx,docx,xlsx]>=0.1.6,<0.2
# ⚠️ playwright 必须 pin 死，且 minor 版本与 pnpm-lock.yaml 中 server 的 playwright 对齐（当前 1.58.x → chromium 构建号 1208）。
# /ms-playwright 浏览器二进制由 Dockerfile 里 Node 侧 `playwright install chromium`（版本锁在 pnpm-lock）预装；
# Python 包若浮动升级会期望更高的 chromium 构建号 → sandbox 内 browser skill 报 "Executable doesn't exist"。
# Dockerfile acs-deps stage 有构建期自检（acs-orchestrator/scripts/verify_playwright_browsers.py），版本不齐会 fail-fast。
playwright==1.58.0
