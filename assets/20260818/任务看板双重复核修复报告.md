# 任务看板双重复核修复报告

- 日期：2026-08-18
- 复核基线：`ed7f65db82900c6a10ef804191bd544a872c20b3`
- 最终集成基线：`1c69daa780dd3272d4e18e6d2287acefcc53590a`
- 范围：独立复核 A 的 11 项 finding、独立复核 B 的 9 项 finding
- 结论：**20/20 已关闭；无已知 P0/P1/P2 遗留。**

## 1. 独立复核 A：状态一致性

| Finding | 处理结果 | 关键证据 |
|---|---|---|
| P1-1 merge fact 未覆盖 claim/continuation | 已修复。claim、continuation enqueue/running/completion 均在 Task 锁内加载权威 merge fact；已合并事实吸收迟到操作。 | `workflow/commandService.ts`、`storeExecutionLifecycle.ts`、`continuationStore.ts`、PG provider-receipt-only 回放 |
| P1-2 blocked 可绕过 Resume | 已修复。普通 move 拒绝工作流保护态迁移；blocked 只允许显式 resume command，integration 必须指定 sourceIds。 | `store.ts`、`workflow/resumeService.ts`、route/tool/Web 测试 |
| P1-3 stale_subject 与 merge finalizer 反向锁序 | 已修复。统一 Task→Source/Attempt→Execution 锁序；PR 记录与 Resolution 提交不再反向获取锁。 | `deliveryPullRequests.ts`、`integrationOperations.ts`、PG 并发回放 |
| P1-4 provider merge 前 TOCTOU | 已修复。provider 调用前重新验证 execution、lane、authorization、task/source 与 merge operation；取消/过期主体 fail-closed。 | `integrationOperations.ts`、定向单测与 PG 回放 |
| P1-5 remediation pointer/attempt 分叉 | 已修复。唯一 active Source/Attempt 绑定在同一事务中写入；冲突重放只接受同一 canonical attempt。 | `integrationOperations.ts`、`remediationAttempts` schema、PG 原子性测试 |
| P1-6 脏数据会跳过 active PR 唯一约束 | 已修复。迁移先识别异常，repair 可审计修复；短名 `_apr_uq` 避免 PostgreSQL 标识符截断。 | `v2Schema.ts`、repair PG 测试 |
| P1-7 repair 漏 fence provider-receipt-only | 已修复。三类 merge fact 共用判定；修复 Task/Source projection、fence Execution 并写 cancellation outbox，post-check 同谓词。 | `repairTaskboardWorkflow.ts`、PG dry-run/apply 幂等测试 |
| P2-1 Resolution digest 非 canonical JSON | 已修复。递归稳定排序后计算 digest；语义相同、键序不同的 replay 幂等。 | `workflow/commandService.ts` 及单测 |
| P2-2 receipt 缺少 runId | 已修复。receipt 持久化并校验 runId、execution/attempt、purpose、workflow epoch、fence。 | shared types、`workflow/commandService.ts`、receipt 负向测试 |
| P2-3 repair task-id/prefix 语义错误 | 已修复。关联任务 scope 显式处理；复用 Store 前缀限制，超长前缀在连接数据库前拒绝。 | repair 脚本单测与 PG 定向测试 |
| P2-4 缺少真实 PG 并发/迁移验证 | 已关闭。隔离 PostgreSQL 18.1 上六个 Taskboard PG 文件合并执行，33/33 通过。 | workflow/store/V2/continuation/race/lifecycle PG suites |

## 2. 独立复核 B：产品全链路

| Finding | 处理结果 | 关键证据 |
|---|---|---|
| P1-1 integration 恢复无选择地全选来源 | 已修复。仅 `needs_human` 来源可勾选，默认不选；API 只收到用户选中的 sourceIds。 | `TaskDetail.tsx`、`IntegrationSources.tsx` 及 Web 测试 |
| P1-2 canceled source 仍投影 claimed | 已修复。canceled source 保留历史导航，但 delivery 恢复 eligible，可重新加入集成批次。 | `storeHelpers.ts`、`storeSearch.ts`、V2/PG 测试 |
| P1-3 claim 幂等重放晚于状态检查 | 已修复。先完成授权与对象可见性，再按 executionId/runId 双键幂等命中；双键分叉 fail-closed；新 claim 仍受终态与 active guard。 | `storeExecutionLifecycle.ts`、PG replay 测试 |
| P2-1 切换任务残留上一任务来源 | 已修复。taskId 变化与加载失败均清空来源状态，并对迟到请求结果做隔离。 | TaskBoard hooks/IntegrationSources 测试 |
| P2-2 历史 Resolution 未迁移 | 已修复。唯一可绑定 legacy change 投影为 historical canonical Resolution；重复/残缺记录显式标异常，不伪造 applied。 | `v2Schema.ts`、PG migration fixture |
| P2-3 repair task-id 绑定错误别名 | 已修复。delivery/integration/remediation 关联 scope 分别解析并审计。 | repair 脚本及定向 PG 测试 |
| P2-4 三类任务关系不可导航 | 已修复。返回 identifier/title；delivery、integration、remediation 详情支持双向导航与修复轮次历史。 | shared types、`storeTaskAccess.ts`、`IntegrationSources.tsx`、`TaskDetail.tsx` |
| P2-5 创建弹窗提供非法初始状态 | 已修复。仅 backlog/todo 可普通创建；in_progress 必须显式“直接执行”；其他工作流状态不展示。 | `TaskDialog.tsx` 及契约测试 |
| P2-6 PG/RBAC/恢复未形成门禁 | 已关闭。新增负向 RBAC、terminal replay、多来源恢复、remediation、迁移、repair 与真实 PG 覆盖。 | Server 135、Web 71、PG 33 全部通过 |

## 3. 最终验证

```text
pnpm typecheck
  PASS：server/shared/web

Taskboard Server 非 PG
  PASS：21 files / 135 tests

TaskBoard Web
  PASS：9 files / 71 tests

Taskboard PostgreSQL 18.1（隔离测试库）
  PASS：6 files / 33 tests

pnpm build
  PASS（仅既有 Browserslist、动态/静态导入与 chunk size 警告）

pnpm check:ratchets
  PASS：14/14 ratchet tests，max-lines/env-var budgets 通过

git diff --check
  PASS
```

## 4. 边界

- 未连接、读取或修改生产/共享数据库。
- repair 只在每次测试创建并销毁的隔离数据库执行。
- 未 push、未部署、未发送外部消息。
- `projects/task-84-taskboard-comment-filter/` 未修改、未删除、未纳入提交。
