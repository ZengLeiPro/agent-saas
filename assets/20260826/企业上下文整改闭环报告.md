# 企业上下文整改闭环报告

- 工作树：`projects/agent-saas-context-repair`
- 分支：`fix/context-plane-product-repair`
- 初始基线：`origin/main@ca7dd827`
- 日期：2026-08-26
- 状态：**已本地提交并无冲突 rebase 到执行时最新 `origin/main`；尚未推送、建 PR、部署或执行生产复验。**

## 1. 结论

原独立复核中的后端 3 个 P1、5 个 P2、1 个 P3，以及前端 4 组 P1、3 组 P2，已完成代码层整改。没有发现仍可由普通成员远程利用的 P0/P1 授权缺陷。

仍有三类发布前 caveat：

1. 未连接真实 PostgreSQL 验证 `NORMALIZE(..., NFKC)` 的生产 locale、执行计划及表达式 CPU 成本。
2. Assignment、advisory lock、批量 JSONB 写入和同事务 projection outbox 尚未做真实 PG 并发/故障注入。
3. 兼容 projection receipt 只显示 durable `pending`，没有终态轮询；Assignment 是权威生效面，产品验收通过 Context Center、Timeline、Entities 和新 Agent 会话完成。

## 2. 后端复核问题闭环

| 原发现 | 状态 | 整改与证据 |
|---|---|---|
| P1-1 ACL 存在性/数量侧信道 | 已关闭 | Recall DTO 不再携带 scanned/authorized/denied 数量；Agent tool 也不输出 diagnostics。Entity 统一为 `no_visible_records`。`server/src/context/retrieval/types.ts`、`server/src/context/retrieval/postgresContextRecallService.ts`、`server/src/agent/contextSearchToolProvider.ts` |
| P1-2 Entity 最多 1000 次串行 ACL | 已关闭 | `authorizeRecords` 批量鉴权，Entity 每批一次 authorizer 调用。`server/src/context/product/authorization.ts:43`、`server/src/context/product/service.ts:132` |
| P1-3 Assignment 与 outbox 不原子 | 已关闭 | Assignment 写与全部 projection outbox 使用同一 PG client/事务；任一 enqueue 失败整体回滚且 `changed:false`。`server/src/data/assignments/store.ts:96`、`server/src/data/assignments/store.ts:116`、`server/src/routes/governanceAssignmentBatchRoutes.ts:141` |
| P2-1 NFKC 只归一请求 | 已关闭（待真实 PG） | SQL 比较端同步使用 `NORMALIZE(..., NFKC)` + `LOWER`。`server/src/context/retrieval/postgresContextRecallService.ts:169` |
| P2-2 `kind` 破坏旧语义 | 已关闭 | `kind` 保持 storage `record_kind`，业务类型放入 `entityType`，并保留 `recordKind`。`server/src/context/retrieval/postgresContextRecallService.ts:385` |
| P2-3 外部 offset cursor | 已关闭 | Entity 使用 actor/tenant/fingerprint/Assignment version 绑定的 `cp2` keyset cursor。`server/src/context/product/authorization.ts:121`、`server/src/context/product/service.ts:151` |
| P2-4 suites 滚动发布不兼容 | 已关闭 | suites 仅在 `includeSuites=1` 返回；新客户端显式请求并对旧服务缺失字段默认空数组。`server/src/routes/governanceMemoryRoutes.ts:179`、`shared/src/lib/governanceApi.ts:587` |
| P2-5 25 万规则与串行 INSERT | 已关闭 | Batch 路由与 Store 双层限制总规则 500；每资源规则使用一次 `JSONB_TO_RECORDSET` 批插，资源仍按稳定锁顺序串行处理。`server/src/routes/governanceAccessValidation.ts:76`、`server/src/data/assignments/store.ts` |
| P3-1 恰好 LIMIT 误报 degraded | 已关闭 | Recall/Timeline/Entity 均采用 `limit+1` 判定 hasMore/capped。`server/src/context/retrieval/postgresContextRecallService.ts`、`server/src/context/product/service.ts` |

## 3. 前端与产品复核问题闭环

| 原发现 | 状态 | 整改与证据 |
|---|---|---|
| P1-1 Taskboard 缺件伪装完整套件 | 已关闭 | 服务端 manifest 返回 expected/missing/unknown/completeness；缺 required 禁止简单提交，未知 Taskboard Collection 明确告警。`server/src/routes/governanceMemoryRoutes.ts:76`、`web/src/components/OrganizationGovernance/KnowledgeSuiteSetup.tsx` |
| P1-2 高级治理矩阵不完整 | 已关闭 | 高级编辑支持 everyone/user/directory_group/agent × allow/deny；用户、群组、Agent 使用权威目录；失效对象保留显示；未改规则保留 origin。`web/src/components/OrganizationGovernance/MemoryKnowledgeGovernance.tsx` |
| P1-3 回执/投影失败生命周期 | 已关闭原缺陷，保留 caveat | Outbox 已与 Assignment 原子化；成功 receipt 提升到父级，reload 期间不丢；展示 durable pending 与 Context 验收入口。尚无 projection 终态轮询。`web/src/components/OrganizationGovernance/MemoryKnowledgeGovernance.tsx`、`web/src/components/OrganizationGovernance/KnowledgeSuiteSetup.tsx` |
| P1-4 群组/Agent 名称与人数不真实 | 已关闭 | Preview 从权威目录解析 user/group/Agent label；按 everyone/user/group allow/deny 展开并去重计算 before/after/added/removed effective users；Agent 规则单列计数。`server/src/routes/governanceAssignmentBatchRoutes.ts` |
| P2-1 snapshot 失败伪装为空 | 已关闭 | snapshot 独立 loading/error；失败明确显示“同步状态不可用，不等于没有同步记录”。`web/src/components/OrganizationGovernance/KnowledgeSuiteSetup.tsx` |
| P2-2 URL 仅恢复顶层页签 | 已关闭 | URL 绑定 tenant，并恢复 view/filter/entity；支持 back/forward 和同页多实例同步；参数纳入 URL contract guard。`web/src/components/OrganizationGovernance/OrganizationGovernancePage.tsx:486`、`web/src/components/ContextCenter/ContextProductPanels.tsx:110` |
| P2-3 异步目录测试稳定失败 | 已关闭 | 测试等待成员/群组权威目录完成；新增目录失败、套件缺件、复杂规则、snapshot 失败与 URL 恢复覆盖。`web/src/components/OrganizationGovernance/OrganizationGovernancePage.test.tsx` |
| 新会话动作缺失 | 已关闭 | 提交回执提供“新建 Agent 会话”入口，并保留 Context Center/Timeline/Entities 验收入口。`web/src/components/OrganizationGovernance/KnowledgeSuiteSetup.tsx` |
| 签名信息不可核验 | 已关闭 | previewId/baselineDigest 移入高级签名详情。`web/src/components/OrganizationGovernance/KnowledgeSuiteSetup.tsx` |

## 4. 验证结果

### Context 定向回归

- Server 核心定向：此前 131 项全绿；整改后受影响文件继续复跑，最近一轮 30/30 通过。
- Shared governance API：36/36 通过。
- Web Organization/Context/URL/icon：最近相关组合 45/45、33/33 通过。
- Assignment batch：4/4 通过，覆盖 500 规则上限、真实群组人数、outbox 故障整体回滚。

### 全仓覆盖

- Server 574 个测试文件按 4 shard 覆盖：5510 项通过、188 项按环境跳过、3 项 todo。两项并发资源敏感失败单文件复跑 66/66 通过。
- Shared 全量：58 个文件、848/848 通过。
- Web 226 个测试文件按 2 shard 覆盖：1735 项在分片与失败项复跑后通过。发现并修复 2 项本次新增 icon guard 违规；BoardDialog/TaskDetail 的默认 5 秒阈值在高并发下超时，单独/15 秒阈值复跑通过。

### 门禁

- Server / Shared / Web typecheck：通过。
- Web production build：通过。
- Web API boundary：通过。
- Web startup budget：通过（startup JS 1 request / 304376 gzip bytes；CSS 1 request / 33438 gzip bytes）。
- Ratchet：16/16 通过。
- max-lines / env-var ratchet：通过。
- icon guard / URL contract guard：通过。
- `git diff --check`：通过。

### 非 Context 阻碍

- `check:comparison-layout` 在当前验证环境中两次卡在 Playwright `page.goto(..., waitUntil=load)` 固定 30 秒超时；Vite fixture 在此前 fetch 探测已可达。未修改该无关测试基础设施。
- 既有 Dialog Description、React `act(...)`、Browserslist 陈旧和 >500 kB chunk 警告仍存在。

## 5. 发布前必须处理

1. 已将本次提交无冲突 rebase 到执行时最新 `origin/main`，并在 rebase 后重跑 Context 定向测试、三包 typecheck、Web build、API boundary、startup budget 与 ratchet。
2. 需要真实 PostgreSQL 集成验证 NFKC SQL、批量 JSONB 写入、advisory lock 与 outbox 同事务故障注入。
3. 未经用户进一步确认，不 push、建 PR 或部署。
4. 部署后必须新建 Agent 会话，不复用旧验证会话 `b616db9a-f117-53c9-88af-944c3cbb6ca8`。
5. 不重复提交生产已存在的三个 Taskboard v2 Assignment。
6. 生产复验需覆盖 Timeline、Entities、Recall、Evidence 一致性，以及撤权、跨租户、authorizer 异常和伪造 Evidence handle 的 fail-closed。
