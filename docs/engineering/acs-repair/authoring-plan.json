{
  "id": "native-control-image-wiring-v1",
  "baseSha": "29736ac84f9f3dcc470fd178f961516b43b6f15b",
  "files": [
    {
      "path": "acs-orchestrator/src/sandboxManifest.ts",
      "extract": {"sourcePath": "acs-orchestrator/src/sandboxManager.ts", "start": "  private buildSandboxManifest(ref: SandboxRef): Record<string, unknown> {", "end": "  /** Sandbox CR + Pod 联动删除"},
      "prefix": "import type { AcsOrchestratorConfig } from './config.js';\nimport type { SandboxRef } from './sandboxManagerTypes.js';\nimport { APP_LABEL, CREATED_AT_ANNOTATION, LAST_ACTIVE_AT_ANNOTATION, MANAGED_BY_LABEL, MOUNT_SUBPATH_ANNOTATION, NETWORK_POLICY_DENY_PRIVATE_ANNOTATION, NETWORK_POLICY_MODE_ANNOTATION, NETWORK_POLICY_MODE_LABEL, SANDBOX_SCOPE_ANNOTATION, SANDBOX_SCOPE_LABEL, SESSION_ANNOTATION, SESSION_LABEL, SHARED_READ_ONLY_SUBPATH_ANNOTATION, WORKSPACE_ANNOTATION, WORKSPACE_LABEL } from './sandboxInventoryReader.js';\nimport { buildWorkspaceVolumeMounts } from './sandboxWorkspaceMounts.js';\nimport { WORKLOAD_CLASS_LABEL, WORKLOAD_DESCRIPTOR_ANNOTATION } from './sandboxLifecyclePolicy.js';\nimport { sandboxResourceTarget } from './sandboxResourceDrift.js';\nimport { acsNetworkPolicyMode, labelValue, nodeHeapLimitMb } from './sandboxState.js';\nimport { buildPackageMirrorEnv, buildSandboxProxyEnv, egressSandboxFingerprint } from 'server/runtime/egressPolicy.js';\n\nconst ACS_NETWORK_POLICY_AGENT_ANNOTATION = 'network.alibabacloud.com/enable-network-policy-agent';\nconst ACS_NETWORK_POLICY_MODE_ANNOTATION = 'network.alibabacloud.com/network-policy-mode';\nconst EGRESS_FINGERPRINT_ANNOTATION = 'agent-saas.kaiyan.net/egress-fingerprint';\nconst SANDBOX_TIMEZONE = 'Asia/Shanghai';\n\n",
      "transforms": [
        {"before": "  private buildSandboxManifest(ref: SandboxRef): Record<string, unknown> {", "after": "export function buildSandboxManifest(config: AcsOrchestratorConfig, ref: SandboxRef): Record<string, unknown> {"},
        {"before": "this.config", "after": "config", "all": true},
        {"before": "      ...(config.pvcName ? {\n        volumeMounts: buildWorkspaceVolumeMounts(config.workspaceMountPath, mountSubPath, sharedReadOnlySubPath),\n      } : {}),", "after": "      volumeMounts: [\n        ...(config.pvcName ? buildWorkspaceVolumeMounts(config.workspaceMountPath, mountSubPath, sharedReadOnlySubPath) : []),\n        // Kernel-supplied Pod identity, not a caller environment value or workspace file.\n        { name: 'acs-identity', mountPath: '/var/run/acs-identity', readOnly: true },\n      ],"},
        {"before": "        ...(config.pvcName ? {\n          volumes: [{ name: 'workspace', persistentVolumeClaim: { claimName: config.pvcName } }],\n        } : {}),", "after": "        volumes: [\n          ...(config.pvcName ? [{ name: 'workspace', persistentVolumeClaim: { claimName: config.pvcName } }] : []),\n          { name: 'acs-identity', downwardAPI: { defaultMode: 0o444, items: [\n            { path: 'pod-uid', fieldRef: { apiVersion: 'v1', fieldPath: 'metadata.uid' } },\n          ] } },\n        ],"},
        {"before": "\n  ", "after": "\n", "all": true}
      ]
    },
    {
      "path": "acs-orchestrator/src/sandboxManager.ts",
      "replacements": [
        {"before": "import { sandboxImageRef } from './sandboxImageRef.js';", "after": "import { sandboxImageRef } from './sandboxImageRef.js';\nimport { buildSandboxManifest } from './sandboxManifest.js';"},
        {"beforeFrom": {"sourcePath": "acs-orchestrator/src/sandboxManager.ts", "start": "  private buildSandboxManifest(ref: SandboxRef): Record<string, unknown> {", "end": "  /** Sandbox CR + Pod 联动删除"}, "after": "  private buildSandboxManifest(ref: SandboxRef): Record<string, unknown> {\n    return buildSandboxManifest(this.config, ref);\n  }\n\n"}
      ]
    },
    {
      "path": "acs-orchestrator/src/sandboxRunner.ts",
      "replacements": [
        {"before": "import { runSandboxRunnerDaemon } from './sandboxRunnerDaemon.js';", "after": "import { handoverSandboxControl } from './sandboxControlEntry.js';"},
        {"beforeFrom": {"sourcePath": "acs-orchestrator/src/sandboxRunner.ts", "start": "  if (process.argv.includes('--daemon')) {", "end": "  const raw = await readStdin();"}, "after": "  // No Node parent may retain the control pipe while same-UID tools execute.\n  // --owned-child is launched only by the immutable per-attempt supervisor.\n  if (!process.argv.includes('--owned-child')) {\n    handoverSandboxControl(process.argv.includes('--daemon') ? 'daemon' : 'oneshot');\n  }\n"}
      ]
    },
    {
      "path": "acs-orchestrator/src/remote/runner_daemon.py",
      "replacements": [
        {"before": "return [\"node\", str(candidate), \"--owned-child\"]", "after": "return [\"/usr/local/bin/node\", str(candidate), \"--owned-child\"]"},
        {"beforeFrom": {"sourcePath": "acs-orchestrator/src/remote/runner_daemon.py", "start": "    candidate = HERE.parent / \"sandboxRunner.ts\"", "end": "\n\ndef unknown("}, "after": "    raise RuntimeError(\"prebuilt sandbox worker is required; workspace loader fallback is forbidden\")"}
      ]
    },
    {
      "path": "acs-orchestrator/src/remote/attempt_supervisor.py",
      "replacements": [
        {"before": "        self.transferred = False", "after": "        self.transferred = False\n        self.terminal_resource: str | None = None"},
        {"before": "        if self.cancelled_at is not None or self.transferred:\n", "after": "        if self.cancelled_at is not None or self.transferred or self.terminal_resource in (\"stopped\", \"not_started\"):\n"},
        {"before": "        envelope = self.state(resource, proof=proof, **({\"background\": background} if background else {}))", "after": "        envelope = self.state(resource, proof=proof, **({\"background\": background} if background else {}))\n        self.terminal_resource = resource"}
      ]
    },
    {
      "path": "acs-orchestrator/package.json",
      "replacements": [
        {"before": "esbuild src/index.ts src/backgroundShellWorker.ts src/restorePerPodCli.ts", "after": "esbuild src/index.ts src/sandboxRunner.ts src/backgroundShellWorker.ts src/restorePerPodCli.ts"},
        {"before": " && rm -rf descriptions && cp -R ../server/src/agent/descriptions descriptions", "after": " && node scripts/copy-native-control.mjs && rm -rf descriptions && cp -R ../server/src/agent/descriptions descriptions"}
      ]
    },
    {
      "path": "Dockerfile",
      "replacements": [
        {"before": "COPY acs-orchestrator/src acs-orchestrator/src", "after": "COPY acs-orchestrator/src acs-orchestrator/src\nCOPY acs-orchestrator/scripts acs-orchestrator/scripts"},
        {"before": "--target=node20", "after": "--target=node22", "count": 2, "all": true},
        {"before": "    && test -s dist/backgroundShellWorker.mjs \\\n    && cp -R ../server/src/agent/descriptions ./descriptions", "after": "    && test -s dist/backgroundShellWorker.mjs \\\n    && node scripts/copy-native-control.mjs \\\n    && /usr/local/bin/python3 -I -m compileall -q dist/remote \\\n    && cp -R ../server/src/agent/descriptions ./descriptions"},
        {"before": "| node dist/sandboxRunner.mjs \\\n    && node -e", "after": "| node dist/sandboxRunner.mjs --owned-child \\\n    && node -e"},
        {"before": "    && printf '{\"kind\":\"ping\",\"nonce\":\"docker-build-smoke\"}\\n' | node dist/sandboxRunner.mjs --daemon | grep '\"kind\":\"daemon_pong\"' \\", "after": "    && mkdir -p /var/run/acs-identity \\\n    && printf 'image-smoke-only\\n' > /var/run/acs-identity/pod-uid \\\n    && printf '{\"kind\":\"ping\",\"nonce\":\"docker-build-smoke\"}\\n' | node dist/sandboxRunner.mjs --daemon | grep '\"kind\":\"daemon_pong\"' \\\n    && rm /var/run/acs-identity/pod-uid && rmdir /var/run/acs-identity \\", "count": 1},
        {"before": "    && chown -R agent:agent /opt/uv-cache\n", "after": "    && chown -R agent:agent /opt/uv-cache \\\n    && su agent -c '/usr/local/bin/python3 -I /app/acs-orchestrator/dist/remote/native_smoke.py'\n"}
      ]
    }
  ]
}
