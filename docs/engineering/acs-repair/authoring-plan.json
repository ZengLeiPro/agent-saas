{
  "id": "pre-abort-guards",
  "baseSha": "194b959f1251df778e30ae193610821ba718c878",
  "files": [
    {
      "path": "acs-orchestrator/src/persistentRunner.ts",
      "replacements": [
        {
          "before": "  ): AsyncIterable<RunnerOutput> {\n    await this.start();",
          "after": "  ): AsyncIterable<RunnerOutput> {\n    // A request cancelled before admission must not start even the shared daemon.\n    if (signal.aborted) return;\n    await this.start();"
        }
      ]
    },
    {
      "path": "acs-orchestrator/src/kubectl.ts",
      "replacements": [
        {
          "before": "  signal: NodeJS.Signals | null;\n}",
          "after": "  signal: NodeJS.Signals | null;\n  /** Transport observation only; local completion is never remote-stop proof. */\n  remoteState?: 'unknown' | 'not_started';\n}"
        },
        {
          "before": "  async run(args: string[], options: { input?: string; timeoutMs?: number } = {}): Promise<KubectlResult> {\n    return await new Promise<KubectlResult>((resolve) => {",
          "after": "  async run(args: string[], options: { input?: string; timeoutMs?: number; signal?: AbortSignal } = {}): Promise<KubectlResult> {\n    if (options.signal?.aborted) {\n      return { stdout: '', stderr: 'kubectl request aborted before spawn', exitCode: -1, signal: null, remoteState: 'not_started' };\n    }\n    return await new Promise<KubectlResult>((resolve) => {"
        },
        {
          "before": "  spawn(args: string[], options: { input?: string; signal?: AbortSignal } = {}) {\n    const child = spawn(",
          "after": "  spawn(args: string[], options: { input?: string; signal?: AbortSignal } = {}) {\n    options.signal?.throwIfAborted();\n    const child = spawn("
        }
      ]
    }
  ]
}
