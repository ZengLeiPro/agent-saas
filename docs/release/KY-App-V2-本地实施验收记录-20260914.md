# KY App V2 本地实施验收记录（2026-09-14）

## 结论

WP0～WP7 已按依赖顺序完成本地实现；WP8 已完成发布前的本地代码门禁，并补齐激活前对业务能力清单和当前用户身份的真实探测。当前成果可进入独立 Staging 验收，但本记录不代表远端 PR、CI、部署、真实组织授权或生产验收已经完成。

## 工作包提交

| 工作包       | 本地分支                               | 提交                                  |
| ------------ | -------------------------------------- | ------------------------------------- |
| WP0          | `feat/ky-app-v2-wp0-security-contract` | `1ba13ef66`、`83c1e645e`              |
| WP1          | `feat/ky-app-v2-wp1-contract`          | `3ebc3acbf`                           |
| WP2          | `feat/ky-app-v2-wp2-platform`          | `7c8f62c57`、`e2c30c420`、`c036eccf2` |
| WP3          | `feat/ky-app-v2-wp3-enrollment-ui`     | `1a377e9d4`                           |
| WP4          | `feat/ky-app-v2-wp4-sdk`               | `5c87ed1c0`                           |
| WP5          | `feat/ky-app-v2-wp5-scaffold`          | `e09deeb20`                           |
| WP6          | `feat/ky-app-v2-wp6-activation`        | `852ebe79c`                           |
| WP7          | `feat/ky-app-v2-wp7-key-lifecycle`     | `0259471da`                           |
| WP8 本地门禁 | `feat/ky-app-v2-wp8-acceptance`        | `7ddbb6cda`                           |

这些分支是顺序叠加的本地工作树，尚未推送。远端 PR #698 仍只代表此前已推送的 WP0 状态。

## 本地证据

- 平台 KY App 回归：55 个测试文件通过，319 项通过、62 项按外部环境条件跳过。
- 契约包：194/194；V2 向量自检通过 7 个正向签名与 38 个负向定义。
- 业务系统接入包：使用本地 PostgreSQL 运行，21 个测试文件、217/217 通过。
- 命令行验收工具：12 个测试文件、75/75 通过。
- 脚手架：16/16 通过；先构建契约包后，脚手架构建通过。
- Web：379 个测试文件、2821/2821 通过；类型检查、生产构建、页面布局和管理后台契约检查通过。
- Server：类型检查与生产构建通过。
- 仓库规则：45 项规则测试、文件行数和环境变量数量门禁通过。

本机 Node.js 为 `22.21.1`，仓库固定版本为 `22.23.1`，执行期间有版本告警但上述命令均正常退出。完整 macOS preflight 中仍存在仓库既有的 Linux 专用 `flock`、`timeout`、`/proc` 测试不兼容；不能把该项记为通过，应由 GitHub Linux CI 复核。

## WP8 尚待明确授权的真实验收

以下动作会触及远端、部署环境、真实组织或密钥，按仓库规则没有在本次自动执行：

1. 推送 WP0 修复与 WP1～WP8 本地分支，并为每个工作包建立独立 PR。
2. 运行或修改 GitHub Workflow、等待同一最终提交的 Linux CI。
3. 冻结 Staging 的 RC、平台 SHA、业务系统 SHA、SDK 摘要和业务能力清单摘要。
4. 部署隔离测试业务系统并执行浏览器一键授权，读取授权前后 PID。
5. 回读平台与业务系统状态，完成真实只读能力、无权限用户、跨组织用户、密钥轮换、多副本、断连、响应丢失、重放、撤销和回滚演练。
6. Staging 全部证据通过后，再单独讨论 Production。

没有完成上述真实验收前，不对外宣称“已上线”或“组织已经可以无重启接入”。
