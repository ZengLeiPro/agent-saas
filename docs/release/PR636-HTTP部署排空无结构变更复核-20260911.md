# PR #636 HTTP 部署排空变更：无数据库结构变更复核

本记录补充发布可靠性修复，不批准新数据库迁移，不放宽发布 postcondition、ConfigIdentity 或人工处置门禁。

## 固定源码与差异

受审文件：`server/src/runtime/httpTransport.ts`。
受审目标内容 SHA-256：`92fa15d8ec3674f1a09feded49b5008b5218bb641bb8dd40e2b939dc5506a4a6`，对应 PR 提交 `87c1eb5645529c47938fed9278994c7e513cfc0c`。
补充核对基线：`a7da7be10303c701e4ef49e6f2162d9da7155e13`；其原记录只覆盖身份 JSON 存储，不能代表后续 HTTP transport 变更已审核。

该基线至目标的实际差异只有：默认部署排空等待由 10 分钟调整为 21 分钟；对明确未开始执行的 ACS deployment-draining 503 读取剩余预算，按本地原截止时间与远端剩余时间加 60 秒启动宽限取最小值。无新增导入、SQL、DDL、迁移入口、数据回填或启动建表调用。请求重试仍要求 execution-started=false，重复响应不能延长已经确立的截止时间。

因此该文件归类为 no-schema-change。既有 40 条记录只更新过其目标摘要；本次另为上述遗漏基线补充独立文件条目，并为 41 条相关记录追加本证据摘要。各记录的其他文件、基线摘要、分类及原审核证据保持不变。任何受审源码或本证据字节变化均应重新审核，不能按路径通配放行。

## 验证依据与范围

隔离运行 `34548844419` 的 PostgreSQL job 在目标 `87c1eb56` 上通过了 HTTP 部署等待与 RuntimeDrainState 共 8 项测试、两次真实双 Worker 交棒，以及流式 live/replay/done 场景。该运行同时显示上述基线遗漏的源码分类仍然阻断门禁；补充审核后还须重新执行完整历史分类检查。

这些结果不证明真实 ACS/Kubernetes/NAS/SNAT 已通过发布验收。真实 RC 构建和生产变更仍须执行迁移计划、数据库 postcondition 和运行态身份检查。

## 证据字节维护

本文经仓库锁定版本的格式化器规范化；41 条引用记录均绑定规范化后的实际字节。此维护不改变受审 HTTP 源码、迁移分类或任何发布准入条件。
