# PR636 当前 PR 基线的 HTTP 排空迁移复核

## 身份与范围

- 基线：`cc99440d43cdfcb22795260835614fe53eef8035`（含 PR634、PR635）。
- 受审目标：`719da36cd14452fe334ce2453f92788e18414df5`。
- 唯一受审路径：`server/src/runtime/httpTransport.ts`。
- 基线文件摘要：`sha256:36e39fcaf36b1b57201150eed1fbda8e5bfc342a82c1c0c281df52b5f842ff85`。
- 目标文件摘要：`sha256:92fa15d8ec3674f1a09feded49b5008b5218bb641bb8dd40e2b939dc5506a4a6`。

## 逐段复核结论

两端差异只有两处：部署排空等待上限从 10 分钟调整为 21 分钟；在明确证明尚未执行的部署排空 503 分支中读取 `x-acs-drain-remaining-ms`，以 `Math.min` 收紧本地 deadline。新增语句只读取响应头、转换数字和计算时间，不执行 SQL、DDL、数据回填或启动建表；import、持久化格式、schema 初始化入口及其依赖均未改变。原有幂等标识、取消以及禁止重放未知执行结果的边界没有在这两处差异中被删除。

因此，本文件在上述两端字节之间分类为 `no-schema-change`。这不是对 HTTP 超时策略的全面生产验收，也不授予其他文件迁移放行权限。

## 本次失败原因与修复边界

CI #3719 的静态检查使用当前 PR base 与目标提交做真实依赖闭包比较。历史审核任务只遍历登记过的历史基线；历史任务成功不等于当前 PR base 已受审。历史记录还绑定其他文件的两端内容，不能因为 HTTP 文件恰好匹配就部分复用整条记录。

本次仅追加一条当前基线、单一路径、双端摘要和本复核文档摘要绑定的记录，保留原有所有审核记录，不修改迁移分类器、审核加载器、数据库后置条件或静态门禁。后续 HTTP 字节或本证据变化必须重新审核。新增回归在没有 GitHub event 的本地运行中也显式比较上述真实基线，并验证源文件、证据篡改及证据缺失仍被拒绝。

## 验证与生产边界

隔离验证先复现未修复的真实 PR-base 失败，再运行修复后的同一测试、字节绑定回归、完整静态检查和历史迁移审核。执行结果以 GitHub Actions 日志和最终 PR CI 为准；本文不预先声明运行成功。没有合并 PR、部署生产或修改生产数据库；云端在途任务、ACS、OSS/CDN/PWA 全链路验收仍单独保留。
