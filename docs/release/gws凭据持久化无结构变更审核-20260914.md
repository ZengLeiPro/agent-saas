# gws 凭据持久化无结构变更审核

## 审核范围

- 生产基线：`6db0d0cbb138ec2d1c900871e7decf8047cf63a2`（`origin/main`，TASK-411 分支起点）。该 blob 与历史基线 `cc99440d` / `9db36e88` / `eec01d4c` 中的两份目标源码字节相同。
- 目标文件：`server/src/connectors/googleWorkspace.ts`、`server/src/connectors/runtimeState.ts`。
- 关联任务：TASK-411（ACS sandbox 重建后 gws 401）。

## 结论

本次变更分类为 `no-schema-change`。两份文件都没有新增、修改或删除 SQL、DDL、迁移版本、启动建表、数据库后置条件、数据回填或数据删除。`googleWorkspace.ts` 里既有的 PostgreSQL OAuth state store 与 Vault 读写路径未改；本次只调整 `resolveGoogleWorkspaceRuntimeEnv`，让每次 Run 额外注入 `GOOGLE_WORKSPACE_CLI_KEYRING_BACKEND=file`，短期 access token 仍只来自既有 `accessToken()`。`runtimeState.ts` 只把暂停隔离从 dws/飞书扩展到 gws：覆盖 `GOOGLE_WORKSPACE_CLI_CONFIG_DIR` 到 `/tmp/agent-saas-paused/.../gws`，并删除 token / credentials file env。JS `delete` 操作符删除的是进程 env 键，不是 SQL `DELETE`。

镜像侧 `Dockerfile` 同步固定 file keyring，让 `credentials.enc` 与 `.encryption_key` 落在 `/workspace/.gws/`；Dockerfile 不在本审核的数据库闭包内。refresh token 仍只存在 SecretVault，不进入 sandbox env。

不需要 `release-migration: expand`、数据库 postconditions 或独立 contract release。

## 验证证据

- `server/src/__tests__/googleWorkspaceConnector.test.ts`：有 token / 无 token / 无 service 时都带 file keyring backend。
- `server/src/__tests__/feishuRuntimeEnv.test.ts`：暂停 Google Workspace 时切走 CONFIG_DIR，并去掉 TOKEN 与 CREDENTIALS_FILE。
- `server/src/__tests__/runtimeGovernanceCredentials.test.ts`、`handEnvAllowlist.test.ts`：跨进程 Vault 注入与 hand env 放行 KEYRING_BACKEND。
- `pnpm -F server typecheck` 通过。

源码摘要、基线摘要和本文摘要逐条绑定在 `config/release-migration-reviews.json`。任一受审源码或证据发生字节变化都必须重新审核。本文只记录源码与数据库结构判断，不代表生产已经发布。
