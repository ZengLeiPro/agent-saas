# 平台演示模式 V49 迁移审核（2026-09-16）

## 审核范围

本审核覆盖平台演示模式引入的治理库 expand 迁移，以及登记顺序文件的字节变化：

- `server/src/data/governance-schema/latestMigrations.ts`
- `server/src/data/governance-schema/v49PlatformDemoMigration.ts`
- `shared/src/types/governance.ts`（治理 persona 字面量扩展，无 SQL）

## 结论

V49 为 expand-only：新建 `membership_capability_grants` 与 `platform_demo_sessions` 两张表及索引、检查约束；不删除表或列，不回填既有数据。`latestMigrations.ts` 仅追加版本 49 登记，不直接执行或内嵌 SQL，分类为 `no-schema-change`。`shared/src/types/governance.ts` 只扩展 `governancePersonaSchema` 字面量，无数据库语句，分类为 `no-schema-change`。

## 历史组合复核

对每一份历史审核，若相对该基线 `shared/src/types/governance.ts` 有字节变化，则补入 `no-schema-change` 项（仅 persona 字面量扩展）。

仅当既有登记项的 `targetDigest` 精确等于本审核前态（`origin/main` 上 `server/src/data/governance-schema/latestMigrations.ts` 摘要 `sha256:8adbeec1f158cc83fe11c1b83efaecf7223a6d6a9df4103c5fa4db071d879f16`）时，才把该路径推进到本审核目标摘要 `sha256:09860ca9844f25453f8f43ed40e3b21f14e45177e31d26ce8d10f51550f47cb0`，并补入相对该历史基线不存在的 `server/src/data/governance-schema/v49PlatformDemoMigration.ts` expand 项。其他文件、分类、理由与原证据保留。

## 后置条件

`config/release-migration-postconditions.json` 绑定 `server/src/data/governance-schema/v49PlatformDemoMigration.ts`：回读两张新表、主键/唯一约束相关列，以及 capability 检查约束存在。

应用回滚只关闭 `PLATFORM_DEMO_MODE_ENABLED` 并停止授予；不得删除新增表。本审核不代表迁移已部署。
