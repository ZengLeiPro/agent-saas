# 模型流修复无结构变更审核

## 审核范围

PR #491 为模型响应流增加字节预算、截止时间和有限恢复。本记录只审核数据库迁移分类，不替代运行期行为测试，也不授权生产发布。

逐文件核对生产基线 `9f8531e697e68f39d07e846872586dad841125ca` 和 PR 基线 `77592b55171df5ee16516d3fdb5c590b0576c1ed` 到本批源码的差异；三个文件在两组基线上的差异相同。精确两端摘要及证据摘要登记在 `config/release-migration-reviews.json`，源码变化即重新审核。

## 逐文件结论

| 文件                                                          | 差异与结论                                                                                                                                                                         |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server/src/runtime/responses/codexResponsesWebSocketPool.ts` | WebSocket 响应入队前的字节累计、语义进展超时、取消时清理连接与恢复次数约束；删除和替换的是内存连接/计时器逻辑，不是迁移。无 SQL、数据库客户端或结构修改。分类 `no-schema-change`。 |
| `server/src/runtime/responses/responsesStreamBudget.ts`       | 新增内存预算计数器、事件分类和保护错误；Map、Buffer 字节计算与纯事件处理，不读写数据库。分类 `no-schema-change`。                                                                  |
| `server/src/runtime/responses/responsesTransport.ts`          | 仅为 TypeScript 传输接口增加可选的恢复标志和连接失效回调，无新增执行代码或数据库操作。分类 `no-schema-change`。                                                                    |

## 验证证据

本批已有预算/协议与恢复回归测试；其中 `responsesStreamBudget.test.ts`、`codexResponsesWebSocketPool.test.ts` 和 `codexStreamGuardTransport.test.ts` 随本记录绑定文件摘要。模拟模型事件的测试不是生产模型验收。

迁移审核仍执行真实 Git 两端的依赖闭包分析和精确摘要校验；保留原生产基线的全部审核条目与证据。没有添加通配豁免，没有改动迁移分类器、CI 配置或 expand/contract 规则。
