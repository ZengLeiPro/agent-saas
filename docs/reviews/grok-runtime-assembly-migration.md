# Grok runtime assembly: historical migration re-review

Reviewed source base: `eec01d4c1d043a3de0eec54f9fc1ab8d64651c4b`. Complete feature changes for exactly five previously audited source files were re-examined after the historical migration check rejected their stale target digests. No source or evidence wildcard is approved.

| Source                                               | Reviewed change                                                                                                                                                                 | Database impact                                                                                                                                                                                                                     |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server/src/app/runtime.ts`                          | Moves Codex setup into `createModelSubscriptionRuntime`, adds Grok manager/catalog to model/title/quota factories, hooks hot-update invalidation and signed credential rotation | Keeps existing Codex initialization; adds the separately reviewed Grok state and refresh-journal stores through the shared assembly. No inline SQL, backfill or data deletion. Historical `expand` classifications remain `expand`. |
| `server/src/release/configIdentity.ts`               | Adds Grok credentialRef/credentialRefs and endpoint redaction to existing explicit identity projection                                                                          | Metadata projection and digest only; no SQL or database schema changes.                                                                                                                                                             |
| `server/src/release/environmentSafety.ts`            | Applies trusted Grok OAuth/subscription host checks and reuses the existing deployment OAuth mode/host policy for first authorization                                           | Pure environment/config admission; does not execute a database migration or relax a safety gate.                                                                                                                                    |
| `server/src/runtime/responses/responsesTransport.ts` | Adds the Grok transport/continuation literals and optional request-bound continuation method                                                                                    | Type/interface contract only; actual stored events keep the existing storage structure. Reader compatibility is addressed in the rollout guide.                                                                                     |
| `server/src/app/runtimeContracts.ts`                 | Adds Grok credential, device-auth and catalog service fields to the runtime contract                                                                                            | Type-only composition; no DDL or data rewriting.                                                                                                                                                                                    |

The exact main-to-feature diffs were read. The assembly still uses the existing event-store pool, shared Vault and trusted network fetch. Codex table names/DDL are verified by the source-attributed schema fixture; Grok additive tables are verified by `grokSchemaPostconditions.pg.test.ts` and byte-bound read-only SQL postconditions. `grokRuntimeEntrypoints.test.ts`, `grokRuntimePersistence.test.ts` and `grokProductionPublication.test.ts` exercise the resulting consumers, persistence admission and signed publication/recovery. These are isolated executable tests, not production deployment evidence.

Only the target digests of these existing review entries are advanced. Baseline digests, original classifications/reasons and previous evidence remain. The new explanation and tests are additionally digest-bound. Existing database postcondition records for the same source/old-target pairs are retained and copied to the reviewed new target; Grok's new schema is independently checked at its three schema providers. No missing postcondition is waived.

The previous cascade of unrelated contract errors was caused by a failed whole-review byte check, not permission to reclassify the rest of the repository. `check-reviewed-migrations.mjs` and the source closure classifier are unchanged. Run that complete historical inventory again against the final commit; any other mismatch must be separately investigated.

2026-09-12 queue recovery re-review: `server/src/app/runtime.ts` now passes the already initialized shared `PgEventStore` to the background-task missing-session terminalization path and forwards an explicit failure reason. Both changes only wire existing runtime dependencies and metadata values; they add no SQL, DDL, startup migration, backfill, or persistence-shape change. The historical classifications remain unchanged.

The same queue recovery change also updates the exact reviewed bytes of three existing runtime sources:

| Source                                                   | Reviewed change                                                                             | Database impact                                                                                                               |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `server/src/runtime/background/backgroundTaskRuntime.ts` | Distinguishes parent-owned direct subagents from scheduler-owned background coordinators    | Read-only JSONB metadata predicates; `COALESCE` makes missing `subagent` explicitly false. No schema or data mutation.        |
| `server/src/runtime/runStoreQueries.ts`                  | Reuses the existing lease-blocker predicates through one shared SQL fragment                | The lease CAS keeps the same tables, columns and write shape. No DDL, migration, backfill or destructive operation.           |
| `server/src/runtime/runStoreRecoveryQueries.ts`          | Excludes only direct parent-owned subagents while retaining background coordinator recovery | Read-only recovery selection over existing Run metadata and steering rows. No schema change, startup write or data rewriting. |

The newly reached queue modules (`backgroundTaskService.ts`, `platformIncidentPolicy.ts`, `rawRuntimeRunDispatchTypes.ts`, `runLeaseEligibility.ts`, and `scheduler.ts`) were inspected in the same pass. They either compose existing stores/types or execute read/update paths against the existing Run/EventStore schema; none defines startup DDL, changes a persisted shape, performs a backfill, or removes data.
