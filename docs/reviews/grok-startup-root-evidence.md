# Grok startup-root evidence re-review

The complete `scripts/release/migration-plan.mjs` diff against `main@eec01d4c1d043a3de0eec54f9fc1ab8d64651c4b` is exactly one startup-root inventory replacement: the old Codex state implementation is now a compatibility export, and the two actual initializers are `subscriptionCredentialRuntimeState.ts` and `subscriptionRefreshJournal.ts`. The latter adds the independently reviewed Grok refresh journal. The migration classification algorithm, import-closure analysis, deletion/rewiring rejection, review validation, required postconditions and release gates are unchanged.

A full inventory of all source and evidence records found one remaining stale binding: baseline `b4b5968740dba7a9e7dc2681c1c8a6d012d32fa5` retained the pre-refactor migration-plan evidence digest. The old digest was `sha256:64b72aab325049835813417134c5f071164a81a5392bb7d0faac3b99ff051cd8`; the reviewed digest is `sha256:e9af03df61510f49dcf3329294783185530ca367c6603e9ff70d30c43da3d438`. Only that evidence binding advances, with this independent explanation added. Baseline source identities, classifications and all other evidence remain unchanged.

Existing startup-root detector/equality tests still require the inventory to match executable initializers. Source-attributed Codex DDL preservation, Grok additive SQL postconditions and negative missing-index/check tests remain mandatory. The entire historical source inventory, not just current main, must pass `check-reviewed-migrations.mjs` at the final SHA. This is a source/evidence review, not an execution of production migration or a waiver of database postconditions.
