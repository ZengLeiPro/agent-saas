// @kaiyan/ky-app-browser —— 定制项目前端 SDK（无框架依赖）。
// Phase A 只落骨架；握手状态机、令牌单飞续期、fetch 包装、路由同步、openAgent /
// openLink / permChanged 在 Phase B 实现。

/** 当前实现的契约版本，与 @kaiyan/ky-app-contract 的 CONTRACT_VERSION 保持一致。 */
export const CONTRACT_VERSION = 1 as const;

/** 占位：Phase B 用真实实现替换本文件的导出。 */
export const KY_APP_BROWSER_PLACEHOLDER = true;
