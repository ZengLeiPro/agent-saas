// @kaiyan/ky-app-server —— 定制项目服务端 SDK。
// Phase A 只落骨架；verifySat / verifyLocalToken / issueAttestation / directoryClient /
// eventsHandler / defineCapabilities / breakGlass 与 Hono 适配器在 Phase B 实现。

/** 当前实现的契约版本，与 @kaiyan/ky-app-contract 的 CONTRACT_VERSION 保持一致。 */
export const CONTRACT_VERSION = 1 as const;

/** 占位：Phase B 用真实实现替换本文件的导出。 */
export const KY_APP_SERVER_PLACEHOLDER = true;
