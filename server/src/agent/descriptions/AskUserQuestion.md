Pause the agent loop and ask the user one or more questions (each with 2-4 options).
For multiSelect=true questions, the answer for that question key is an array of selected option labels.
For multiSelect=false, the answer is the single selected label string. Omit multiSelect when single-select is intended; the runtime defaults it to false.
