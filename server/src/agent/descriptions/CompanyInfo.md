读取或更新当前租户的共享公司档案（`company.md`）。这是组织级上下文，不是个人记忆。
`action="read"`：读取档案，免审批。平台管理员可传 `tenantId`；租户用户只能读取自己所在租户的档案。
`action="update"`：整体替换档案，仅组织/平台管理员可用，且必须经过人工审批。仅当组织管理员要求更新组织级背景上下文时使用；务必先 read 当前内容，谨慎准备完整替换稿（`content` 必填），等待明确批准后再执行。
