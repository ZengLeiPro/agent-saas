管理当前用户的定时任务，以及任务看板 Execution 的当前任务。target 默认 cron：支持 list/create/update/delete/run，沿用现有 schedule、payload、notify 参数。target=taskboard 只允许 runId 为 taskboard-* 的看板 Agent 调用，普通 Session 不可进入；服务端还会按 runId 反查并锁定 Execution：list 必须带当前任务 id，返回任务、评论与执行记录；update 只能带当前任务 id 和 branch（null 表示清除）；create 必须带当前 boardId+title，只能创建 todo 后续任务，dispatch=true 会立即派发新的 work Agent；只有 review Execution 可 move 当前任务到 done 或 todo；不能 execute 已有任务。创建独立复核、返工或合并 Agent 时使用 create+dispatch。任务看板写操作自动读取最新 version 做 CAS，review 的 move 会在同一数据库事务校验 Execution 仍活跃。cron 的 list 与 taskboard 的 list 无需审批；taskboard 的可逆写回按工作区写入风险处理，create+dispatch 与 cron 写操作保持高风险审批。