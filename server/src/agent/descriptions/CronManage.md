管理当前用户的定时任务与任务看板。target 默认 cron；target=taskboard 时按当前 Execution/Session 识别任务与身份。任务 Agent 使用 execution.comment({body}) 写入所有交付、证据和阻塞信息，随后使用 execution.transition({status}) 仅指定下一 Task 状态；transition 前必须已有当前 Execution 的显式 Agent 评论。Integration Work 创建直接父提交后必须调用 execution.integration_candidate.push（只传 commitOid），不得自行 git push。work、review、merge 可选目标由 workflow contract 的 allowedStatuses 给出。普通会话仍按租户、看板角色和 allowedActions 访问资源。
