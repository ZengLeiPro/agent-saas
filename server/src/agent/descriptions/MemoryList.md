List memory files (MEMORY.md + memory/**.md) in the current workspace with size and last-modified time.
Use this when you do not know the exact path of a memory file before calling MemorySearch or Read.
Optional `subdir` restricts the listing to a memory subdirectory (e.g. "topics", "private").
