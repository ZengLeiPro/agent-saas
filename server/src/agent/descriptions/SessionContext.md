检索本会话的完整历史事实源（EventStore，含每次工具调用的原始输入输出）。当上下文投影省略了较早内容、或需要核对历史细节时使用。action="events" 按时间顺序分页读取事件；action="search" 跨事件搜索；action="trace" 只在指定 toolCallId 的原始工具结果中读取，支持 startLine+lineCount 按行、startChar+charCount 按字符、query+maxMatches+contextLines 按关键词定位。trace 同时收到多组读取参数时按 query、startLine、startChar 的顺序选用一组并忽略其余默认字段；不指定模式时返回开头的有界片段。均为只读操作。
