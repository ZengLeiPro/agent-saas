推进专用隔离环境中的 Workflow Demo 下一步。只传平台创建的 workflowRunId、平台告知的当前 eventId，以及写动作执行前刚刚读取到的 expectedVersion；不要自填目标状态、动作摘要或业务结果。批准、客户回复和恢复信号必须由另一身份从外部提交，不能用本工具代替。运行进入 waiting 后立即停止；外部信号到达后，在新一轮 Agent 运行中继续。只有工具返回完成且业务对象已回读为目标状态，才能声称该步完成。
