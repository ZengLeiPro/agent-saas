import {describe,expect,it} from 'vitest';
import {passesGoalHardGates,progressFingerprint,reduceNoProgress} from './sessionAutomationEvaluator.js';
describe('automation evaluator reducers',()=>{it('requires frozen evidence and every hard gate',()=>{const evidence={summary:'done',evidenceRefs:['run:test'],hardGates:{runTerminal:true,noPendingInteraction:true,noActiveResources:true,budgetValid:true}};expect(passesGoalHardGates(evidence)).toBe(true);expect(passesGoalHardGates({...evidence,evidenceRefs:[]})).toBe(false);expect(passesGoalHardGates({...evidence,hardGates:{...evidence.hardGates,noActiveResources:false}})).toBe(false);});it('fingerprint is deterministic across evidence order',()=>{expect(progressFingerprint({summary:'x',evidenceRefs:['b','a']})).toBe(progressFingerprint({summary:'x',evidenceRefs:['a','b']}));});it('pauses only at no-progress threshold and resets on progress',()=>{expect(reduceNoProgress('x','x',1,3)).toEqual({count:2,pause:false});expect(reduceNoProgress('x','x',2,3)).toEqual({count:3,pause:true});expect(reduceNoProgress('x','y',8,3)).toEqual({count:0,pause:false});});});
