export type {
  Tenant,
  CreateTenantInput,
  UpdateTenantInput,
  TenantSettings,
  UserInfo,
} from "@agent/shared";
export { DEFAULT_TENANT_ID, DEFAULT_TENANT_SETTINGS, TENANT_SLUG_PATTERN } from "@agent/shared";
