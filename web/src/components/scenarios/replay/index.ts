export {
  allReplayScripts,
  getReplayScript,
  getWorkflowCardReplayScript,
} from '@agent/shared/scenarios/replay/registry';
export { hasReplayScript } from '@agent/shared/scenarios/replay/availability';
export type { ReplayScript, ReplayStep, SourceRegistration } from '@agent/shared/scenarios/replay/types';
