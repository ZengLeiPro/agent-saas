# Setup

## 1. Install CLI

```bash
cd ~/.ky-agent/skills/browser && bash setup.sh
```

## 2. Verify

```bash
playwright-cli --version
```

The platform automatically configures `PLAYWRIGHT_MCP_CDP_ENDPOINT` for each user. No manual browser or token setup is needed.
